t.onsubmit=async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(t));const r=await fetch('/api/order/track',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(b)}).then(x=>x.json());if(r.error)return alert(r.error);document.getElementById('r').innerHTML=`<h3>${r.product_name}</h3><p>Order ID: ${r.order_id}</p><p>Amount: ₹${r.amount}</p><p>Status: ${r.custom_status}</p><h4>Updates</h4>${r.updates.map(x=>`<p>${x.update_text}</p>`).join('')}`}
