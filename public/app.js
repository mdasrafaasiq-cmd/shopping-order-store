(async()=>{const s=await fetch('/api/settings').then(r=>r.json());shop.textContent=s.shop_name||'My Shop'; const p=await fetch('/api/products').then(r=>r.json()); const render=()=>{const q=search.value.toLowerCase();products.innerHTML=p.filter(x=>x.name.toLowerCase().includes(q)).map(x=>`<article><h2>${x.name}</h2><p>₹${x.price}</p><a href="/product.html?code=${encodeURIComponent(x.code)}">View Product / Order</a></article>`).join('')};search.oninput=render;render()})()
